# DiskANN vs HNSW 邻居访问次数对比

本报告对比了DiskANN和HNSW在搜索过程中的邻居访问效率，通过统计距离计算次数和节点访问次数来分析Vamana Graph的设计动机。

## 测试指标

- **距离计算次数** (Distance Computations): 每次查询调用distance函数的次数
- **节点访问次数** (Nodes Visited): 搜索过程中访问的不同节点数量
- 在相同召回率约束下对比这两个指标

---

## 1. Fashion-MNIST数据集对比

**数据集信息**：
- 训练集：60,000个向量
- 维度：784
- 距离度量：Euclidean（欧式距离）

### 90%召回率约束

| 方法 | 召回率 | 平均距离计算 | 平均节点访问 | QPS |
|------|--------|-------------|-------------|-----|
| **DiskANN** (L=3) | 94.25% | 406.5 | 406.5 | 5,997 |
| **HNSW** (ef=7) | 90.40% | 881.7 | 881.7 | 2,087 |
| **DiskANN效率** | - | **2.2×更少** | **2.2×更少** | **2.9×更快** |

### 95%召回率约束

| 方法 | 召回率 | 平均距离计算 | 平均节点访问 | QPS |
|------|--------|-------------|-------------|-----|
| **DiskANN** (L=4) | 96.14% | 422.9 | 422.9 | 5,388 |
| **HNSW** (ef=9) | 95.25% | 926.1 | 926.1 | 1,847 |
| **DiskANN效率** | - | **2.2×更少** | **2.2×更少** | **2.9×更快** |

### 99%召回率约束

| 方法 | 召回率 | 平均距离计算 | 平均节点访问 | QPS |
|------|--------|-------------|-------------|-----|
| **DiskANN** (L=15) | 99.38% | 574.8 | 574.8 | 3,026 |
| **HNSW** (ef=30) | 99.35% | 1240.3 | 1240.3 | 1,218 |
| **DiskANN效率** | - | **2.2×更少** | **2.2×更少** | **2.5×更快** |

---

## 2. COCO数据集对比

**数据集信息**：
- 训练集：113,287个向量
- 维度：512
- 距离度量：Angular（余弦距离）

### 90%召回率约束

| 方法 | 召回率 | 平均距离计算 | 平均节点访问 | QPS |
|------|--------|-------------|-------------|-----|
| **DiskANN** (L=4) | 90.08% | 489.8 | 489.8 | 4,701 |
| **HNSW** (ef=7) | 90.40% | 1092.3 | 1092.3 | 1,637 |
| **DiskANN效率** | - | **2.2×更少** | **2.2×更少** | **2.9×更快** |

### 95%召回率约束

| 方法 | 召回率 | 平均距离计算 | 平均节点访问 | QPS |
|------|--------|-------------|-------------|-----|
| **DiskANN** (L=8) | 95.33% | 550.7 | 550.7 | 3,458 |
| **HNSW** (ef=15) | 95.56% | 1322.4 | 1322.4 | 1,318 |
| **DiskANN效率** | - | **2.4×更少** | **2.4×更少** | **2.6×更快** |

### 99%召回率约束

| 方法 | 召回率 | 平均距离计算 | 平均节点访问 | QPS |
|------|--------|-------------|-------------|-----|
| **DiskANN** (L=150) | 99.02% | 2195.5 | 2195.5 | 794 |
| **HNSW** (ef=100) | 98.76% | 2597.0 | 2597.0 | 588 |
| **DiskANN效率** | - | **1.2×更少** | **1.2×更少** | **1.4×更快** |

---

## 3. 关键发现

### 邻居访问效率对比

**一致性观察**：
- DiskANN在所有测试场景下，距离计算次数和节点访问次数**完全相等**
- HNSW在所有测试场景下，距离计算次数和节点访问次数也**完全相等**
- 这说明两种算法都对每个访问的节点恰好计算一次距离

**效率差异**：
- **低召回率场景（90-95%）**：DiskANN访问节点数约为HNSW的**40-45%**（2.2-2.4倍更少）
- **高召回率场景（99%）**：差距缩小，DiskANN访问节点数约为HNSW的**80-85%**（1.2倍更少）

### 为什么DiskANN访问更少的节点？

**HNSW的多层结构开销**：
1. **层级导航成本**：HNSW需要从顶层逐层向下搜索
2. **上层节点访问**：虽然上层稀疏，但仍需访问额外节点进行导航
3. **累积效应**：每一层的节点访问都会累加到总访问次数中

**DiskANN的单层优势**：
1. **直接搜索**：Vamana图是单层结构，直接在完整图上进行贪婪搜索
2. **高质量连接**：通过RobustPrune算法（alpha参数）确保图的连通性和多样性
3. **无层级开销**：不需要层间导航，所有访问都直接服务于找到最近邻

---

## 4. Vamana Graph的设计动机

基于实验结果，DiskANN提出Vamana Graph的核心动机是：

### 动机1：减少冗余访问

**问题**：HNSW的多层结构导致节点访问冗余
- 上层节点访问主要用于"导航"而非"精确搜索"
- 层间跳转增加了额外的距离计算开销

**解决方案**：Vamana使用单层高质量图
- 通过RobustPrune确保图的连通性，无需多层结构
- 所有节点访问都直接贡献于找到最近邻

### 动机2：简化算法复杂度

**问题**：HNSW需要管理多层图结构
- 层级分配算法（随机level）
- 每层独立的邻居选择策略
- 层间搜索策略

**解决方案**：Vamana统一为单层贪婪搜索
- 算法更简单，易于实现和优化
- 减少了参数调优的复杂度

### 动机3：保持高召回率

**关键观察**：尽管访问更少节点，DiskANN仍能达到相同甚至更高的召回率
- Fashion-MNIST 99%召回：DiskANN访问574.8个节点，HNSW访问1240.3个节点
- COCO 99%召回：DiskANN访问2195.5个节点，HNSW访问2597.0个节点

**原因**：RobustPrune的多样性参数（alpha）确保了图的质量
- alpha > 1时，优先选择距离远但能覆盖不同方向的邻居
- 避免了邻居聚集在同一区域，提高了搜索的探索能力

---

## 5. 结论

**Vamana Graph的核心优势**：
1. **更高的搜索效率**：在相同召回率下访问更少的节点（1.2-2.4倍）
2. **更快的查询速度**：减少距离计算直接转化为更高的QPS（1.4-2.9倍）
3. **更简单的设计**：单层结构比多层结构更易实现和优化

**设计权衡**：
- 构建时间：DiskANN需要两轮构建（Pass 1 + Pass 2），比HNSW慢
- 但查询性能的提升证明了这个权衡是值得的

**适用场景**：
- 当查询性能比构建时间更重要时，Vamana Graph是更好的选择
- 特别适合需要高吞吐量、低延迟的在线检索系统
